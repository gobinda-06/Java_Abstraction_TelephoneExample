# Java_Abstraction_TelephoneExample
Demonstrates abstraction in Java using an abstract class Telephone. The subclass SmartPhone implements abstract methods ring(), lift(), and disconnect() to show how abstraction defines method structure while subclasses provide concrete behavior.
